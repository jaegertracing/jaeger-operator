# Enable tracking.gaID
apiVersion: jaegertracing.io/v1
kind: Jaeger
metadata:
  name: all-in-one-with-ui-config
spec:
  strategy: allinone
  ui:
    options:
      tracking:
        gaID: "MyTrackingId"
