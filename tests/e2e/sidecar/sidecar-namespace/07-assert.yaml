# Ensure the sidecar is not there anymore
apiVersion: v1
kind: Pod
metadata:
  labels:
    app: vertx-create-span-sidecar
spec:
  containers:
    - name: vertx-create-span-sidecar
status:
  phase: Running
