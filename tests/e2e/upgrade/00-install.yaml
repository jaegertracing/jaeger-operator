# Deploy a Jaeger instance
# The 00-assert.yaml file is generated by the prepare-e2e-kuttl-tests make
# rule. It will try to assert the Jaeger instance uses the current container
# version
apiVersion: jaegertracing.io/v1
kind: Jaeger
metadata:
  name: my-jaeger
spec: {}
