apiVersion: io.jaegertracing/v1alpha1
kind: Jaeger
metadata:
  name: with-sampling
spec:
  version: "123"
  strategy: allInOne
  sampling:
    options:
      default_strategy:
        type: probabilistic
        param: 50
